{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --worker-class=gthread --workers=2 --threads=4 --bind 0.0.0.0:$PORT server:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
